interface:
  display_name: "Autoskills"
  short_description: "Repo-specific patterns and workflows for autoskills"
  default_prompt: "Use the autoskills repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true